import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // output: 'standalone', // Commented out for Netlify (Netlify uses default build)
  /* config options here */
};

export default nextConfig;
