// Deprecated. Activity points are now stored in the `activity_types` table
// and managed via /api/admin/activity-types. This file is kept empty to
// avoid build failures from stale imports; it can be removed once verified.
export {}
